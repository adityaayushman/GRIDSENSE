# Keep npm's cache off C:, which on this machine is full. Without this,
# `npm run build` fails with ENOSPC before Vite is even reached — npm cannot
# write its own cache. Project-local so it travels with the repo.
cache=D:\tmp\npm-cache
